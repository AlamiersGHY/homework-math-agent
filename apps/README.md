# Apps

Application code lives here.

- `web/`: Next.js frontend application scaffold.
- `api/`: FastAPI backend application scaffold.

Each application should own its own source tree, dependencies, test setup, and runtime commands.

Use each app's local README for setup, run, and test commands.
